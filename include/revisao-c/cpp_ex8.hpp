#pragma once // evita múltiplas definições

#include <tuple>

// dica: retorna uma tupla com 2 elementos (veja slides com std::make_tuple)
auto exercicio8(const char *entrada) { return std::make_tuple(0, 0); }
